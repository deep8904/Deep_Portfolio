# The real production URL, once deployed — used for metadataBase, canonical
# URLs, the sitemap, and robots.txt. Falls back to http://localhost:3000 if
# unset, so nothing is silently wrong in dev.
NEXT_PUBLIC_SITE_URL=https://your-domain-here.com
